name: Deploy static site to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
      - run: bun install
      - run: bun run generate
      - name: Normalize static output for Pages
        run: |
          if [ -d ".output/public" ]; then
            echo "Using .output/public"
          elif [ -d "dist" ]; then
            echo "Using dist, copying to .output/public"
            mkdir -p ".output"
            rm -rf ".output/public"
            cp -R "dist" ".output/public"
          else
            echo "No static output folder found. Expected .output/public or dist."
            exit 1
          fi
      - uses: actions/upload-pages-artifact@v3
        with:
          path: .output/public

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
